Characters list:

<div class="row list-wrapper">
  <ul class="list-group">
    {{#each model as |character|}}
      <img src="assets/characters/{{character.name}}.jpg" />
      <li class="list-group-item"><em>{{character.name}}</em></li>
    {{/each}}
  </ul>
</div>

{{outlet}}
<br/>